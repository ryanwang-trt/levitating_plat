CONFIG_I2C=y

# Use the M33's hardware FPU. 
# Hardware FP drops the policy inferences to ~tens of us.
CONFIG_FPU=y

# printf() with %f needs floating-point formatting pulled into the C library.
# Without this, %f prints nothing/garbage on picolibc.
CONFIG_CBPRINTF_FP_SUPPORT=y
